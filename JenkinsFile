node {

  stage("get props") {
    script {
      def props = readProperties file:'test.properties';
    }
  }

  stage("echo") {
    sh 'echo "${properties.name}"'
    sh "echo 'testing'"
    sh "env"
  }
}

