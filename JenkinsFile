#!/usr/bin/groovy

node {

env.WORKSPACE = pwd()

  stage("Setup") {
    deleteDir()
    git url: "https://github.com/benhosmer/shiro-4.git"
  }

  def propsfile = readFile "${env.WORKSPACE}/test.properties"
  def params = Eval.propsfile

  stage("echo") {
    println params
    sh 'echo "${params}"'
    sh "echo 'testing'"
    sh "env"
  }
}

