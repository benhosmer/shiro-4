node {

stage("where") {
  sh "pwd"
  sh "ls -al"
}
env.WORKSPACE = pwd()
def params = readFile "${env.WORKSPACE}/test.properties"


  stage("echo") {
    sh 'echo $params'
    sh "echo 'testing'"
    sh "env"
  }
}

