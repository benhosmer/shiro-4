node {
  stage 'fortify'
  //def mvnHome = tool 'M3'
  //git url: "https://github.com/venicegeo/pz-ingest.git"
  sh "echo 'testing'"
  sh "env"
  //sh "echo ${env.THREADFIX_ID}"
  //withCredentials([[$class: 'StringBinding', credentialsId: 'DEPLOY_KEY', variable: 'THREADFIXKEY']]) {
    //sh "echo ${THREADFIXKEY}"
  //}
}

